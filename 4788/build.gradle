ext {
  // Robot Project Options

  // teamNum = 4788
  // nativeName = "${teamNum}"
  nativeName = '4788'

  includeDesktopSupport = false
  includeSrcInIncludeRoot = true
}

apply from: rootProject.file('robot.gradle') 